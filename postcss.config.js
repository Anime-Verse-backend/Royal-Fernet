/**
 * @fileoverview Archivo de configuración para PostCSS.
 * Define los plugins que se utilizarán para procesar el CSS,
 * en este caso, Tailwind CSS y Autoprefixer para la compatibilidad de navegadores.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
